# Flask Tutorial

## What is Flask?

Flask is a lightweight Python web framework used to build web applications and APIs. It follows a minimal design and provides core features like routing, request handling, and template rendering while allowing developers to add extensions as needed. It is widely used for building small to medium web applications because of its simplicity and flexibility.

---

## Features of Flask

- **Microframework**  
  Lightweight framework with minimal dependencies, giving developers full control over application structure.

- **Werkzeug & Jinja2**  
  Uses Werkzeug for request/response handling and Jinja2 for dynamic HTML templating.

- **Routing**  
  Maps URLs to Python functions using simple decorators.

- **Flexible Database Choice**  
  No built-in ORM, allowing use of tools like SQLAlchemy or raw SQL.

- **API Development**  
  Well-suited for building RESTful APIs and backend services.

- **Development Server**  
  Includes a built-in server for local testing during development.

---

# Introduction

This section introduces Flask, compares it with Django, and shows how to install it on Windows to start building web applications.

## Flask vs Django

# Basics & Installation

## Requirements

Before installing Flask, make sure the following are installed:

- Python 3.x
- pip (Python package manager)

Check installation:

```bash
python --version
pip --version
```

---

## Install Flask on Windows

### Step 1: Create a Project Folder

```bash
mkdir flask_project
cd flask_project
```

---

### Step 2: Create Virtual Environment

```bash
python -m venv venv
```

Activate virtual environment:

```bash
venv\Scripts\activate
```

---

### Step 3: Install Flask

```bash
pip install flask
```

Check installed version:

```bash
pip show flask
```

---

## First Flask Application

Create a file named `app.py`

```python
from flask import Flask

app = Flask(__name__)

@app.route('/')
def home():
    return "Hello Flask!"

if __name__ == '__main__':
    app.run(debug=True)
```

---

## Run Flask Application

```bash
python app.py
```

Output:

```text
 * Running on http://127.0.0.1:5000/
```

Open browser:

```text
http://127.0.0.1:5000
```

You should see:

```text
Hello Flask!
```

---

# Understanding the Code

## Create Flask Application

```python
app = Flask(__name__)
```

Creates the Flask application object.

---

## Routing

```python
@app.route('/')
```

Maps the `/` URL to the `home()` function.

---

## View Function

```python
def home():
    return "Hello Flask!"
```

Returns a response to the browser.

---

## Debug Mode

```python
app.run(debug=True)
```

Enables automatic reload and detailed error messages during development.

---

# Flask Project Structure

```text
flask_project/
│
├── app.py
├── venv/
├── templates/
├── static/
│   ├── css/
│   ├── js/
│   └── images/
```

---

# Common Flask Commands

## Install Package

```bash
pip install package_name
```

## Export Installed Packages

```bash
pip freeze > requirements.txt
```

## Install from requirements.txt

```bash
pip install -r requirements.txt
```

---

# Advantages of Flask

- Simple and easy to learn
- Flexible project structure
- Lightweight and fast
- Large extension ecosystem
- Good for APIs and microservices

---

# Disadvantages of Flask

- Fewer built-in tools
- Requires manual setup for large projects
- Not ideal for very large enterprise applications without proper architecture

---

# Exercise

## Exercise 1

Create a Flask application that displays:

```text
Welcome to Flask
```

when visiting `/`.

---

## Exercise 2

Create another route `/about` that returns:

```text
About Page
```

---

## Exercise 3

Enable debug mode and explain its purpose.

---
